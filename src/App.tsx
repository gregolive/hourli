import { ReactElement } from 'react';
import Header from './components/Header';

const App = (): ReactElement => {
  return (
    <div className='app'>
      <Header />
    </div>
  );
};

export default App;
